# Example environment configuration

# Base URL for API requests. The API published at http://localhost:8080/docs-json
# expone sus endpoints en /api/v1.
VITE_API_URL=http://localhost:8080/api/v1
# Si ejecutas el frontend dentro de Docker junto al backend pwa-tarjeta_joven,
# cambia la URL a http://pwa-tarjeta_joven:8000/api/v1 para aprovechar la red interna.

# Public Google MyMaps embed URL used in the map view. Replace <MAP_ID> with the actual ID.
VITE_MAPS_URL=https://www.google.com/maps/d/embed?mid=<MAP_ID>
